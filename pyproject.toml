[project]
name = "UFCData"
version = "0.7.1"
description = "Obtain UFC data and functions to manipulate it"
requires-python = ">=3.10"

dependencies = [
    "huggingface-hub",
    "pandas",
    "numpy",
    "glicko2",
    "rapidfuzz",
    "matplotlib",
]
